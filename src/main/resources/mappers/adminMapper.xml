<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bendelivery.mapper.AdminMapper">
	<select id="login" resultType="AdminVO">
		select admin_id, admin_pw from admin_table where admin_id = #{admin_id} and admin_pw = #{admin_pw}
	</select>
</mapper>